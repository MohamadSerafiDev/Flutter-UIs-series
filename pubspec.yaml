name: flutter_ui_1
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
    sdk: ^3.8.0

dependencies:
    flutter:
        sdk: flutter
    flutter_svg: ^2.1.0
    gap: ^3.0.1

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^5.0.0

flutter:
    uses-material-design: true

    assets:
        - assets/images/

    fonts:
        - family: Rajdhani
          fonts:
              - asset: assets/fonts/Rajdhani-Regular.ttf
